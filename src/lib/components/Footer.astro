<footer class="site-footer">
    <div class="footer-container">
        <div class="footer-content">
            <a href="https://civic-data.de/impressum/" target="_blank" rel="noopener noreferrer" class="footer-link">Impressum <svg class="footer-external-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"></path><polyline points="15 3 21 3 21 9"></polyline><line x1="10" y1="14" x2="21" y2="3"></line></svg></a>
        </div>
    </div>
</footer>

<style>
    .site-footer {
        width: 100%;
        background-color: var(--color-background-primary);
        padding: var(--spacing-lg) 0;
        margin-top: var(--spacing-2xl);
    }

    .footer-container {
        width: 100%;
        padding: 0 var(--spacing-lg);
        box-sizing: border-box;
    }

    .footer-content {
        display: flex;
        flex-direction: row;
        justify-content: flex-end;
        align-items: center;
        gap: var(--spacing-sm);
        /* Removed flex-wrap to prevent vertical stacking */
    }

    .footer-link {
        color: var(--color-text-primary);
        text-decoration: none;
        font-weight: var(--font-weight-medium);
        transition: color 0.2s ease-in-out;
        white-space: nowrap; /* Prevent text wrapping */
    }

    .footer-link:hover {
        color: var(--color-primary-darker);
    }

    .footer-divider {
        color: var(--color-text-primary);
    }

    .footer-copyright {
        color: var(--color-text-primary);
        font-size: var(--font-size-sm);
        white-space: nowrap; /* Prevent text wrapping */
    }

    .cc-link {
        color: inherit;
        text-decoration: underline;
    }

    .cc-link:hover {
        color: var(--color-primary-darker);
    }

    .footer-external-icon {
        width: 12px;
        height: 12px;
        vertical-align: middle;
        opacity: 0.6;
    }

    .footer-link:hover .footer-external-icon {
        opacity: 1;
    }

    @media (max-width: 600px) {
        .footer-content {
            flex-direction: column;
            justify-content: center;
            text-align: center;
            gap: var(--spacing-xs);
        }
        .footer-divider {
            display: none;
        }
    }
</style>