---
import Tabs from "./Tabs.svelte";

interface Props {
    tabs: Array<{
        title: string;
        content: string;
    }>;
    activeTab?: number;
}

const { tabs, activeTab } = Astro.props;
---

<Tabs client:load tabs={tabs} activeTab={activeTab} />
